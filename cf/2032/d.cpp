#include <bits/stdc++.h>
using namespace std;

int main() {
	int t; cin >> t;
	while (t--) {
		int n; cin >> n;
		// make queries and solve the problem

		// group stuff into buckets?
		for (int i = 2; i < n; i++) {
			cout << "? 1 " << i << endl;
			int r; cin >> r;
			if (r) {
				// we've found the number of buckets
			}
		}

		

	}
}
