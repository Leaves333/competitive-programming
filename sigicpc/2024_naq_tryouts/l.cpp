#include <bits/stdc++.h>
using namespace std;

int main() {

	int r, c;
	cin >> r >> c;

	string board_string[r];
	for (int i = 0; i < r; i++) {
		cin >> board_string[i];
	}

	int time_needed[r];
	pair<int, int> start;
	pair<int, int> end;

	for (int i = 0; i < r; i++) {
		for (int j = 0; j < c; j++) {
		}
	}

	set<pair<pair<int, int>, int>> s;

}
